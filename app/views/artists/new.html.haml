= form_for @artist, html: { class: "form-horizontal" } do |f|
  %fieldset
    %legend
      = t('.title')
    = render "form", form: f
    .form-actions    
      = f.submit t('.create'), class: "btn btn-primary"
      = link_to t('.cancel'), artists_path, class: "btn"