<nav class="navbar navbar-fixed-top navbar-dark bg-inverse">
  <div class="container">
    <%= link_to 'InPact.me with this song!', root_path, class: 'navbar-brand' %>
    <ul class="nav navbar-nav">
      <li class="nav-item <%= active_controller?('songs') ? 'active' : '' %>">
        <%= link_to songs_url, class: 'nav-link' do %>
          <i class="fa fa-music"></i>
          <%= t('navbar.songs') %>
        <% end %>
      </li>
      <li class="nav-item <%= active_controller?('genres') ? 'active' : '' %>">
        <%= link_to genres_url, class: 'nav-link' do %>
          <i class="fa fa-tags"></i>
          <%= t('navbar.genres') %>
        </a>
        <% end %>
      </li>
      <li class="nav-item <%= active_controller?('artists') ? 'active' : '' %>">
        <%= link_to artists_url, class: 'nav-link' do %>
          <i class="fa fa-users"></i>
          <%= t('navbar.artists') %>
        <% end %>
      </li>
      <li class="nav-item dropdown float-xs-right">
        <a class="nav-link dropdown-toggle" href="#" id="supportedContentDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fa fa-globe"></i>
          <%= t('navbar.locales') %>
        </a>
        <div class="dropdown-menu" aria-labelledby="supportedContentDropdown">
          <% I18n.available_locales.each do |locale| %>
            <%= link_to t("common.languages.full.#{locale.to_s}"), request.parameters.merge({ locale: locale }), class: 'dropdown-item' %>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
