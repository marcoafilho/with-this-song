- if object.errors.any?
  #error_explanation
    %div{ class: "alert alert-error" }
      = t '.error', default: 'The form contains'
      = pluralize(object.errors.count, "error")
      %a{ class: "close", href: "#", onclick: "$(this).hide" }
        &times;